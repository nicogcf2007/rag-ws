# --- URLs del Backend ---

# URL base para las llamadas a la API HTTP/HTTPS del backend.
# Usado por axios para endpoints como /ask, /upload-documents, /get-contexts, etc.
# Valor típico para desarrollo local si tu backend corre en el puerto 8000:
REACT_APP_API_URL=http://localhost:8000
# Ejemplo para producción (si tu backend se despliega en Render, por ejemplo):
# REACT_APP_API_URL=https://tu-nombre-backend.onrender.com

# URL base para la conexión WebSocket (WS/WSS) con el backend.
# Usado para recibir actualizaciones de progreso (/ws/progress).
# Usa ws:// para desarrollo local (HTTP) y wss:// para producción (HTTPS).
# Valor típico para desarrollo local:
REACT_APP_WS_URL=ws://localhost:8000
# Ejemplo para producción (usando WebSocket seguro):
# REACT_APP_WS_URL=wss://tu-nombre-backend.onrender.com